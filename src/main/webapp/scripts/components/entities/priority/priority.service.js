'use strict';

angular.module('dutyhelperApp')
    .factory('Priority', function ($resource) {
        return $resource('api/prioritys/:id', {}, {
            'query': { method: 'GET', isArray: true},
            'get': {
                method: 'GET',
                transformResponse: function (data) {
                    data = angular.fromJson(data);
                    return data;
                }
            },
            'update': { method:'PUT' }
        });
    });
